import { View, Text,   } from 'react-native'
import React from 'react'
import Modal from 'react-native-modal'

export   function OhNoModal({visi}) {
  return (
    <Modal isVisible={}>
      <Text>OhNoModal</Text>
    </Modal>
  )
}