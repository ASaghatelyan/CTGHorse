import React, {memo} from 'react';
import {View, StyleSheet} from 'react-native';
import {styles} from './style';
 

export const Thumb = memo(({name}) => {
  return <View style={  styles.rootHigh  } />;
});
